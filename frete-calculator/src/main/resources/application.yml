server:
  port: 8080

spring:
  datasource:
    url: jdbc:mysql://localhost:3306/sigabem_db?createDatabaseIfNotExist=true&useTimezone=true&serverTimezone=UTC
    username: root
    password: root
    driver-class-name: com.mysql.cj.jdbc.Driver
  jpa:
    hibernate:
      ddl-auto: update
